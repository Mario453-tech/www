.phpunit.cache/
.env
.env.*
!.env.example
ENV_FILE.env

# Runtime logs (nie wersjonujemy — zmieniaja sie przy kazdym requescie)
error_log
game_debug.log
*.log

# Pliki sesji PHP (runtime — nie wersjonujemy)
sessions/sess_*

# Plik wersji buildu — generowany przez deploy, nie w repozytorium
# Build version file — generated by deploy, not tracked in repo
assets/version.txt
