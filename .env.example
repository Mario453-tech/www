DB_HOST=localhost
DB_NAME=gra1
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Token resetu OPcache po deployu — ustaw dlugi losowy ciag.
# OPcache reset token used after deploy — set a long random string.
OPCACHE_RESET_TOKEN=
