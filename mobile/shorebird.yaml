# Shorebird OTA configuration
# Generated by: shorebird init (run this locally first!)
# See: https://docs.shorebird.dev/guides/code-push/
#
# SETUP:
#   1. shorebird login
#   2. shorebird init          ← replaces app_id below with your real ID
#   3. shorebird release android  ← first build, install APK on phone once
#   After that: every push to main auto-patches the installed app.

app_id: REPLACE_WITH_YOUR_APP_ID
